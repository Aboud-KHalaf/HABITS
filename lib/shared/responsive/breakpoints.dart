/// Centralized breakpoints for the application.
/// Used to determine the current [ScreenSize] of the device/window.
class Breakpoints {
  Breakpoints._();

  static const double mobileMax = 600.0;
  static const double tabletMax = 1024.0;
  static const double desktopMax = 1440.0;
}
