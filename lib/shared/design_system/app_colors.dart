import 'package:flutter/material.dart';

/// Neo-Brutalist Color System
class AppColors {
  AppColors._();

  // Core Backgrounds
  static const Color background = Color(0xFF131313);
  static const Color surface = Color(0xFF1E1E1E);
  static const Color surfaceElevated = Color(0xFF2C2C2C);

  // Borders
  static const Color border = Color(0xFFFFFFFF);
  static const Color borderSubtle = Color(0xFF555555);

  // Accents (Electric/Neon)
  static const Color electricBlue = Color(0xFF0055FF);
  static const Color neonGreen = Color(0xFF39FF14);
  static const Color alertRed = Color(0xFFFF2A2A);
  static const Color warningYellow = Color(0xFFFFD700);

  // Typography
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFAAAAAA);
  static const Color textInverse = Color(0xFF000000);
}
