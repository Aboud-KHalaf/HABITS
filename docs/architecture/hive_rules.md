# Hive Rules

This document defines the local storage rules for Hive usage.

## Rules
- Access Hive only from data sources
- Keep adapters inside the core/database layer
- Separate habit definitions from completion history

## Notes
- Never read or write Hive directly from UI widgets
- Document every box and adapter here when added
